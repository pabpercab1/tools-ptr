name: Deploy to Azure Web App

on:
  push:
    branches:
      - azure
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build for Azure (Nitro node-server preset)
        env:
          NITRO_PRESET: node-server
          VITE_UPSTREAM_API_URL: ${{ secrets.VITE_UPSTREAM_API_URL }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_DISCORD_REDIRECT_TO: ${{ secrets.VITE_DISCORD_REDIRECT_TO }}
          VITE_DISCORD_LOGIN_ENABLED: ${{ secrets.VITE_DISCORD_LOGIN_ENABLED }}
        run: npm run build

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .output
